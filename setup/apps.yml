trame:
  cmd:
    - python
    - /deploy/stv_explorer.py
    - --host
    - ${host}
    - --port
    - ${port}
    - --authKey
    - ${secret}
    - --server

stv_explorer:
  cmd:
    - python
    - /deploy/stv_explorer.py
    - --host
    - ${host}
    - --port
    - ${port}
    - --authKey
    - ${secret}
    - --server

stv_reconstructor:
  cmd:
    - python
    - /deploy/stv_reconstructor.py
    - --host
    - ${host}
    - --port
    - ${port}
    - --authKey
    - ${secret}
    - --server